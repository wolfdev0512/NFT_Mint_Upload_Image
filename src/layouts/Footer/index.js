import React from "react";
// import style
import { StyledFooter } from "./style.footer";

const Footer = () => {
  return (
    <StyledFooter>
      <div>Footer</div>
    </StyledFooter>
  );
};

export default Footer;
